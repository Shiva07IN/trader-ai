# TraderAI — AI-Powered Indian Stock Market Platform

> ⚠️ **SEBI Disclaimer:** This platform is for educational purposes only. Content generated by AI does not constitute investment advice. Please consult a SEBI-registered investment adviser before making investment decisions.

## 🚀 Quick Start

### Prerequisites
- Node.js 20+
- Python 3.12+
- Docker Desktop
- OpenAI API key

### 1. Clone & Setup Environment
```bash
# Copy environment file
cp .env.example .env
# Edit .env and add your OPENAI_API_KEY
```

### 2. Start Infrastructure (Docker)
```bash
cd infra
docker compose up -d postgres redis chromadb
```

### 3. Start Backend
```bash
cd apps/api
pip install -r requirements.txt
python -m uvicorn app.main:app --reload --port 8000
```

### 4. Start Frontend
```bash
cd apps/web
npm install
npm run dev
```

### Or use the one-click script (Windows)
```
Double-click: start-dev.bat
```

---

## 📁 Project Structure

```
trader-ai/
├── apps/
│   ├── api/              # FastAPI backend (Python)
│   └── web/              # Next.js 15 frontend (TypeScript)
├── infra/
│   └── docker-compose.yml
├── .env.example          # Environment variables template
└── start-dev.bat         # Windows dev startup script
```

## 🌐 URLs (Development)

| Service | URL |
|---|---|
| Frontend | http://localhost:3000 |
| Backend API | http://localhost:8000 |
| API Docs (Swagger) | http://localhost:8000/docs |
| Flower (Celery) | http://localhost:5555 |

## 🔑 Environment Variables

Edit `.env` and fill in:
- `OPENAI_API_KEY` — Required for AI features
- `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` — For Google OAuth
- Database/Redis passwords (defaults work with Docker)

## 🏗️ Tech Stack

| Layer | Technology |
|---|---|
| Frontend | Next.js 15, TypeScript, Tailwind CSS, shadcn/ui |
| Backend | FastAPI, Python 3.12, SQLAlchemy 2.0 |
| Database | PostgreSQL (Neon in prod) |
| Cache | Redis (Upstash in prod) |
| AI | OpenAI GPT-4o + LangChain |
| Tasks | Celery + Redis |
| Vector DB | ChromaDB |
| Auth | NextAuth.js v5 (Google + Email) |
| Stock Data | yfinance (NSE/BSE) |

## 🚢 Deployment

- **Frontend**: Vercel (connect GitHub repo, set env vars)
- **Backend**: Railway (Dockerfile included)
- **Database**: Neon (PostgreSQL)
- **Cache**: Upstash (Redis)

## 📋 Features (Phase 1 MVP)

- [x] Authentication (email + Google OAuth)
- [x] Stock search (50+ NSE stocks)
- [x] Stock profiles with fundamentals
- [x] Technical indicators (RSI, MACD, MAs)
- [x] AI Portfolio Generator (GPT-4o)
- [x] Market indices dashboard
- [x] Watchlist management
- [x] AI stock analysis (SWOT)
- [x] AI chat assistant
- [x] Redis caching
- [x] Rate limiting
- [x] Docker setup

## ⚖️ SEBI Compliance

This platform operates under SEBI (Research Analysts) Regulations, 2014. All AI outputs:
- Are marked as "AI-generated" 
- Include mandatory disclaimers
- Are logged for 5-year audit trail compliance
- Do not constitute personalized investment advice
